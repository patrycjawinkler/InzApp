<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pwinkler.inzapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.MainActivity">
        </activity>

        <activity android:name=".controllers.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".controllers.RegisterActivity">
        </activity>

        <activity android:name=".controllers.RecipesListActivity">
        </activity>

        <activity android:name=".controllers.ShoppingListActivity">
        </activity>

        <activity android:name=".controllers.ProposeRecipeActivity">
        </activity>

        <activity android:name=".controllers.RandomRecipeActivity">
        </activity>

    </application>

</manifest>